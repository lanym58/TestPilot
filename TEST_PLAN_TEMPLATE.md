# TestPilot 测试计划编写指南（给大模型看）

## 用途

本文档指导大模型（如 Claude、GPT 等）为 TestPilot 工具生成符合解析规则的 Markdown 测试计划文件。TestPilot 会解析该文件并转换为结构化的测试管理数据。

## 解析规则

TestPilot 的解析器只识别两种结构：

1. **`## 标题`** — 二级标题，作为测试模块名称
2. **表格行** — 格式为 `| 编号 | 功能项 | 描述 | 测试要点 |`，作为该模块下的测试项

其他内容（普通文本、一级标题、三级标题、列表等）会被忽略，可自由添加注释说明。

## 文件格式模板

```markdown
# {项目名称} 测试计划

## {模块1名称}

| # | 功能项 | 描述 | 测试要点 |
|----|--------|------|----------|
| 1.1 | {功能名} | {功能描述} | {测试要点，用①②③编号分隔} |
| 1.2 | {功能名} | {功能描述} | {测试要点} |

## {模块2名称}

| # | 功能项 | 描述 | 测试要点 |
|----|--------|------|----------|
| 2.1 | {功能名} | {功能描述} | {测试要点} |
| 2.2 | {功能名} | {功能描述} | {测试要点} |
```

## 字段说明

| 字段 | 要求 | 说明 |
|------|------|------|
| **#（编号）** | `X.Y` 格式，必填 | X = 模块序号，Y = 项序号。如 `1.1`、`2.3`。解析器匹配正则：`[0-9]+\.[0-9]+` |
| **功能项** | 必填，简短 | 功能点名称，建议 20 字以内 |
| **描述** | 必填 | 功能的具体说明，描述用户操作和预期行为 |
| **测试要点** | 必填 | 需要验证的具体场景，用 ①②③ 或数字编号列出多个要点 |

## 编号规则

- 模块序号从 1 开始递增：第一个 `## 标题` 下的项用 `1.x`，第二个用 `2.x`
- 项序号在每个模块内从 1 开始：`1.1`、`1.2`、`1.3`...`2.1`、`2.2`...
- 编号必须是 `数字.数字` 格式，否则解析器不识别

## 完整示例

```markdown
# 电商系统 测试计划

## 认证与登录

| # | 功能项 | 描述 | 测试要点 |
|----|--------|------|----------|
| 1.1 | 用户名密码登录 | 输入用户名和密码完成身份认证 | ①正确账号密码登录成功并跳转首页 ②错误密码提示"用户名或密码错误" ③账号锁定后提示剩余解锁时间 |
| 1.2 | 退出登录 | 点击退出按钮清除会话并跳转登录页 | ①退出后Cookie/Token被清除 ②退出后访问受保护页面跳转登录页 ③退出后浏览器后退不能访问已登录页面 |
| 1.3 | 记住密码 | 勾选记住密码后下次自动填充 | ①勾选后关闭浏览器重新打开自动填充 ②不勾选则不保存 ③密码修改后清除已记住的密码 |

## 商品管理

| # | 功能项 | 描述 | 测试要点 |
|----|--------|------|----------|
| 2.1 | 商品列表 | 展示所有商品，支持分页和搜索 | ①默认按创建时间倒序 ②搜索支持商品名称模糊匹配 ③分页切换正确加载数据 ④空数据显示缺省页 |
| 2.2 | 新增商品 | 填写商品信息创建新商品 | ①必填项为空时阻止提交并高亮 ②图片上传支持jpg/png且≤5MB ③价格只允许正数且最多两位小数 |
| 2.3 | 编辑商品 | 修改已有商品信息 | ①打开编辑页正确回显所有字段 ②修改后保存成功返回列表 ③并发编辑冲突时给出提示 |

## 订单流程

| # | 功能项 | 描述 | 测试要点 |
|----|--------|------|----------|
| 3.1 | 下单 | 选择商品、填写收货地址、确认支付 | ①库存不足时阻止下单 ②地址校验手机号格式 ③订单金额=商品价格×数量+运费 |
| 3.2 | 订单列表 | 查看历史订单及状态 | ①按状态筛选正确 ②取消订单需二次确认 ③已发货订单不可取消 |
```

## 注意事项

1. **表头行和分隔行会被忽略**：`| # | 功能项 | ... |` 和 `|----|-----|` 不会被解析为测试项，因为编号列不匹配 `数字.数字` 格式
2. **每个模块前必须有 `##` 标题**：没有模块标题的表格行会被忽略
3. **表格至少4列**：`| 编号 | 功能项 | 描述 | 测试要点 |`，解析器取前4列
4. **不要在编号中用空格**：`1.1` 正确，`1. 1` 不识别
5. **测试要点写在一行内**：表格单元格不支持换行，用 ①②③ 分隔多个要点
6. **可添加自由文本**：模块标题和表格之间可以插入说明文字，不影响解析

## 生成提示词示例

当你需要让大模型为某个项目生成测试计划时，可以这样提示：

```
请为 {项目名称} 生成 TestPilot 格式的测试计划。

要求：
1. 用 ## 二级标题划分测试模块
2. 每个模块下用表格列出功能项，格式：| 编号 | 功能项 | 描述 | 测试要点 |
3. 编号用 X.Y 格式（如 1.1, 1.2, 2.1）
4. 测试要点用①②③编号，覆盖正常流程、异常场景、边界条件
5. 参考这个模板：{粘贴本文件中的完整示例}
```
