# 工作日志 2026-04-28

## 今日完成

### Phase 1: 项目脚手架 + 文件服务（已完成）

1. **项目初始化**
   - 基于 electron-vite boilerplate 创建项目（Vue 3 + TypeScript）
   - 安装依赖：element-plus、pinia、vue-router、markdown-it、uuid
   - 配置 electron-builder（TestPilot 品牌化）
   - 配置 vitest 测试框架

2. **核心类型定义** (`src/main/types.ts`)
   - 定义全部数据类型：WorkspaceConfig、PlanState、BugRecord、FixRecord、TestResult、ActionRecord

3. **文件服务** (`src/main/fileService.ts`)
   - 实现工作空间初始化（创建子目录 + config.json + WORKSPACE_CLAUDE.md）
   - JSON 文件原子写入（写 .tmp 再 rename）
   - Markdown 测试计划解析（## 模块标题 + 表格行）
   - Bug / Fix / TestResult / Screenshot 的完整 CRUD
   - 路径安全校验（assertSafeName 防止路径遍历）

4. **IPC 桥接** (`src/main/ipcHandlers.ts` + `src/preload/index.ts`)
   - 19 个 IPC handler，统一 safe() 包装防止错误泄露
   - preload 通过 contextBridge 暴露类型安全的 API
   - 删除不安全的 importFromPath IPC（只允许 dialog 选择文件）

5. **Pinia Stores**
   - workspaceStore：工作空间打开/关闭
   - planStore：测试计划加载/导入/保存
   - sessionStore：测试会话 + 操作缓冲
   - bugStore：Bug 列表/保存/选择

6. **Vue 渲染层骨架**
   - App.vue：欢迎页 + 侧边栏布局
   - 6 个路由 + 6 个视图占位（Dashboard/PlanList/PlanDetail/TestSession/BugList/BugDetail）

7. **TDD 测试**
   - 12 个 fileService 单元测试全部通过
   - 覆盖：工作空间初始化、JSON 读写、计划导入解析、Bug CRUD、截图、测试结果、修复记录

### 代码审查修复

| 问题 | 级别 | 修复 |
|------|------|------|
| 路径遍历漏洞 | CRITICAL | assertSafeName() 校验所有 ID 参数 |
| importFromPath 读取任意文件 | CRITICAL | 删除该 IPC，只允许 dialog |
| readJson 无异常处理 | HIGH | try-catch，损坏 JSON 返回 null |
| IPC 错误堆栈泄露 | HIGH | safe() 统一包装 |
| 截图截主窗口而非 webview | HIGH | webContents.fromId(id) |
| testPoints 赋值错误 | HIGH | 改用 rowMatch[4] |
| N+1 串行 IPC | HIGH | Promise.all 并行 |
| Windows 路径分隔符 | MEDIUM | split(/[/\\]/) |
| contextIsolation 降级 | MEDIUM | 改为抛出错误 |

### Phase 2: 基础 UI（已完成）

1. **Dashboard 概览页** — 6 个统计卡片（计划数/功能项/通过/不通过/未测/Bug）+ 完成率进度条
2. **PlanList 测试计划列表** — 卡片列表 + 导入 Markdown 对话框 + 统计标签
3. **PlanDetail 计划详情** — 模块折叠表格 + 状态标签 + 每行"开始测试"按钮

### Phase 3: 内嵌浏览器 + 操作录制（已完成）

1. **recorderCore.ts** — 元素标签识别（aria-label > data-testid > label > placeholder > innerText）、事件转 ActionRecord、密码字段脱敏、CSS selector 生成
2. **recorder.ts** — webview 注入脚本：click/input(500ms防抖)/scroll(>=100px)/navigate 捕获 + pushState/replaceState monkey-patch
3. **EmbeddedBrowser.vue** — webview 组件 + 地址栏 + 前进后退刷新 + 动态 preload 路径 + `nextTick` 修复 webview 绑定时序
4. **ActionRecorder.vue** — 操作记录实时侧栏 + 手动步骤添加 + 删除
5. **TestToolbar.vue** — 通过/不通过按钮 + Bug 提交完整表单（标题/严重度/描述/复现步骤预览）
6. **ScreenshotButton.vue** — 截图按钮（通过 webContentsId 截取 webview）
7. **TestSession.vue** — 分栏布局整合所有组件 + 测试要点面板

### TDD 测试新增

| 测试文件 | 测试数 | 覆盖内容 |
|----------|--------|----------|
| actionFormatter.test.ts | 11 | navigate/click/input/scroll/observe 格式化 + 空值/无label |
| markdownParser.test.ts | 8 | 空内容/空模块/5列表格/特殊字符/默认值 |
| recorderCore.test.ts | 18 | extractLabel优先级/密码脱敏/selector生成/scroll过滤 |

### 运行时 Bug 修复

| 问题 | 原因 | 修复 |
|------|------|------|
| safe() 改变返回类型 | 包装为 {ok,data} 但 stores 期望原始值 | 改为直接返回数据，错误返回 null |
| webview 不加载网页 | v-if 导致 onMounted 时 webview 为 null | nextTick + watch 双重绑定 |
| 不通过按钮无反应 | window.prompt 在 Electron 中被阻止 | 改用 Element Plus ElMessageBox |
| 提交 Bug 报 clone 错误 | Pinia reactive Proxy 不能 IPC 序列化 | toRaw + JSON.parse(JSON.stringify()) |
| 提交后跳转离开 | 提交 Bug/通过 后 router.push 跳走 | 留在测试页面，显示成功提示 |

## 验证状态

- `npx vitest run` — 49 tests passed（4 files）✓
- `npx electron-vite build` — build 成功 ✓
- `npm run dev` — 完整流程可跑通：打开工作空间 → 导入计划 → 开始测试 → 内嵌浏览器 → 提交 Bug ✓

## 明日计划

- Phase 4: 截图功能完善 + 测试结果持久化
- Phase 5: Bug 列表/详情页面完整实现
- Phase 6: ClaudeCode 集成 + 快捷键 + 会话草稿保存
